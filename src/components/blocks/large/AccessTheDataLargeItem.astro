---
import SolutionLargeItem from "~/components/blocks/SolutionLargeItem.astro";
interface Props {
  mirror: string;
}
const { mirror } = Astro.props;
---

<SolutionLargeItem
  mirror={mirror}
  title="Access the data behind, when you need it"
  description="Unlike many other tools, Ludi doesn't hide the details away from you. Select any object in Ludi and Copy as JSON to copy the object data into the clipboard. Try building a wireframe, copying it as JSON and asking an LLM to convert it to HTML & Tailwind. The future is now."
  image="~/assets/images/product-outcomes/access-the-data.png"
/>
